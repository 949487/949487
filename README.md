# PoetryCrossword

一个使用 Jetpack Compose 编写的诗词填字示例：

- 24 行 × 20 列棋盘。
- 支持横向 / 纵向经典诗句布局。
- 同一首诗使用统一颜色标记。
- 共享格子自动做交叉验证。
- 空白格保留留白。
- 每完成一首诗，对应弹窗展示题目、作者、全诗与说明。
- `app/src/main/assets/poems_simple.json` 提供了只包含 `诗词 / 要的诗词 / 作者` 的简化 JSON 数据。
